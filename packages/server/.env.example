# Used when running the server directly on the host (npm run dev), against the
# dockerized Postgres (whose port is published to localhost by docker-compose).
DATABASE_URL=postgresql://bookmarks:change-me@localhost:5432/bookmarks
DEEPSEEK_API_KEY=
PORT=3001
