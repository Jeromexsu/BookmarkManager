services:
  app:
    build:
      target: build
    command: npm run dev -w @bookmark-manager/server
    environment:
      NODE_ENV: development
    volumes:
      - ./packages/server/src:/app/packages/server/src
      - ./packages/shared/src:/app/packages/shared/src
